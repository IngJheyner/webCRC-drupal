{#
/**
 * @file
 * Template for a Logo Header
 */
#}
{% set classes = [
  'block',
  bundle ? 'block--' ~ bundle|clean_class,
  id ? 'block--' ~ id|replace({"_": "-"})|clean_class,
]|merge(utility_classes ? utility_classes : []) %}

{% block content %} 
  <div class="my-4 mr-5 text-center">
    {{ content.field_home_header_logo_webcrc }}
  </div>
{% endblock %} 